name: multiclass_classifier_dart
description: "Multiclass Classifier ONNX Test"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  onnxruntime: ^1.4.1
  path_provider: ^2.1.2
  image_picker: ^1.0.7
  image: ^4.1.7
  collection: ^1.18.0
  http: ^1.2.0
  shared_preferences: ^2.2.2
  fl_chart: ^0.60.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    - model.onnx
    - vocab.json
    - scaler.json 